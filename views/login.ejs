<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Impulse Guard</title>
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>

<div class="auth-wrapper">

    <!-- LEFT DECORATIVE PANEL -->
    <div class="auth-left">

        <div class="auth-left-content">
            <p class="brand-label">Impulse Guard</p>

            <h1>
                The <span>Ethereal</span><br>
                Guardian of Your<br>Assets.
            </h1>

            <p class="desc">
                Advanced AI-driven impulse protection. We sit between
                your intent and your capital, ensuring every transaction
                is aligned with your future.
            </p>
        </div>

        <div class="auth-image-box">
            <img src="/images/ai-image.png" alt="AI Graphic" onerror="this.style.display='none'">
        </div>

        <div class="auth-feature-row">
            <div class="auth-feature-card">
                <p class="feat-title">Secure Protocol</p>
                <p class="feat-desc">Multi-layered encryption for every tier.</p>
            </div>
            <div class="auth-feature-card">
                <p class="feat-title">Smart Analytics</p>
                <p class="feat-desc">Real-time spending behavioral patterns.</p>
            </div>
        </div>

    </div>

    <!-- RIGHT FORM PANEL -->
    <div class="auth-right">
        <div class="auth-form-box">

            <h2>Welcome back</h2>
            <p class="sub">Enter your credentials to access your dashboard.</p>

            <!-- Show error if login failed -->
            <% if (locals.error && error) { %>
                <div class="error-banner"><%= error %></div>
            <% } %>

            <!-- Divider -->
            <div class="divider">
                <div class="divider-line"></div>
                Login with email
                <div class="divider-line"></div>
            </div>

            <!-- Login form -->
            <form action="/auth/login" method="POST">

                <div class="field-group">
                    <label for="email">Email Address</label>
                    <input
                        type="email"
                        id="email"
                        name="email"
                        placeholder="admin@mail"
                        required
                    >
                </div>

                <div class="field-group">
                    <div class="field-row">
                        <label for="password">Password</label>
                        <a class="forgot-link" href="#">Forgot?</a>
                    </div>
                    <input
                        type="password"
                        id="password"
                        name="password"
                        placeholder="••••••••"
                        required
                    >
                </div>

                <button type="submit" class="btn-submit">Sign In →</button>

            </form>

            <p class="hint-text">Demo credentials: admin@mail / admin</p>

            <p class="auth-footer">
                Don't have an account?
                <a href="/register">Create Account</a>
            </p>

        </div>
    </div>

</div>

</body>
</html>
